plugin: amazon.aws.aws_ec2

regions:
  - eu-central-1

filters:
  instance-state-name: running
  tag:Monitoring: enabled

compose:
  ansible_host: private_ip_address

hostnames:
  - instance-id

groups:
  monitored: "'Monitoring' in (tags | default({}))"
---
- name: Audit EC2 disk usage
  hosts: monitored
  become: false
  gather_facts: false

  tasks:
    - name: Check disk usage
      ansible.builtin.command: df -hT
      register: disk_usage
      changed_when: false

    - name: Show disk usage
      ansible.builtin.debug:
        var: disk_usage.stdout_lines